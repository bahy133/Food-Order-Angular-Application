import { CustomCartDirective } from './custom-cart.directive';

describe('CustomCartDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomCartDirective();
    expect(directive).toBeTruthy();
  });
});
