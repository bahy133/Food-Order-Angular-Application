import { CustomlangDirective } from './customlang.directive';

describe('CustomlangDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomlangDirective();
    expect(directive).toBeTruthy();
  });
});
