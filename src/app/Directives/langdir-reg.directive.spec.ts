import { LangdirRegDirective } from './langdir-reg.directive';

describe('LangdirRegDirective', () => {
  it('should create an instance', () => {
    const directive = new LangdirRegDirective();
    expect(directive).toBeTruthy();
  });
});
